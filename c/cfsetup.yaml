jessie:
    build:
        builddeps:
            - build-essential
            - ragel
        post-cache:
            - make lib
    test:
        builddeps: &testdeps
            - build-essential
            - ragel
            - nodejs=8.*
        pre-cache:
            - npm i -g tap-summary
        post-cache:
            - make test
    bamboo-test:
        builddeps: *testdeps
        pre-cache:
            - npm i -g tap-xunit
            - chmod o+r -R /usr/local/lib/node_modules
        post-cache:
            - make bamboo-test
